# Some *Giphy* looking fackah

## Install
* Clone or download repo
* Run "npm install" in folder to install all dependencies
* Run "npm run browser" to start only browser-sync or "npm run watch" to also run the watch commands
